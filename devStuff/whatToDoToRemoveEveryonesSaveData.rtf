to remove everyones save data

in javascript ../main.js
in save function 
    'localStorage.setItem("version", 1);'
    change the numeral into one above
in load function 
    'if (localStorage.getItem("version") == 1)'
    change the numeral into one above